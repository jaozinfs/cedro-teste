<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/login_bg_gradient_end_color</item>
    </style>

    <!-- register button -->
    <style name="RegisterButtonStyle" >
        <item name="android:textSize">15dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@color/text_light</item>
        <item name="android:background">@drawable/login_bg_layout_gradient</item>
    </style>

    <!-- lab's login-->
    <style name="EditScreenTextInputLayoutStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/text_light</item>
        <item name="colorControlActivated">@color/login_bg_gradient_center_color</item>
    </style>

    <style name="BottomSheetEd" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="boxStrokeColor">@color/bottosheet_ed_colors</item>
    </style>

</resources>
